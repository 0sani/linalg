#include <iostream>
#include <cassert>
#include <array>
#include "mat.hpp"

template<typename T, size_t rows, size_t cols>
class Basis {
private:
    std::vector<Matrix<T, rows, cols>> elems;
public:


bool linearly_independent() {
    
}

};